# EmailJS configuration for contact form

# Get this from EmailJS Dashboard -> Account -> API Keys
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here

# Email service ID (EmailJS Dashboard -> Email Services)
VITE_EMAILJS_SERVICE_ID=your_service_id_here

# Template ID used to send the email (EmailJS Dashboard -> Email Templates)
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here

# Notes:
# - Template variables expected by the code:
#   from_name -> sender full name
#   reply_to  -> sender email (used for reply-to)
#   message   -> message body
# - Ensure your EmailJS template uses these exact variable names.

